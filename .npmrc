; Vercel runs a clean `npm install` and npm v9/v10 enforces peer dependencies.
; Some dependencies in this repo still declare React 18-only peer ranges (e.g. mathjax3-react),
; even though the app is on React 19. This prevents installs in CI unless we relax peer resolution.
legacy-peer-deps=true


