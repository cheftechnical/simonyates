import * as React from 'react';
import Work from './index';
import {BrowserRouter as Router} from 'react-router-dom';

export default {title: 'Pages/Work'};

export const _default = () => <Router><Work/></Router>;
