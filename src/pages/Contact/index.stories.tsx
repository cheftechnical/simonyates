import * as React from 'react';
import Contact from './index';
import {BrowserRouter as Router} from 'react-router-dom';

export default {title: 'Pages/Contact'};

export const _default = () => <Router><Contact/></Router>;
